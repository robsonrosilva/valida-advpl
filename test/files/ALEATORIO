#Include "TOTVS.CH"

//----------------------------------------------------------
/*/{Protheus.doc} DARFVAL
 Atualiza o vetor com as novas informações alteradas pelo usuário
@author TOTVS BSB
@version PROTHEUS 11
@since -- 
/*/
//----------------------------------------------------------

User Function DARFVAL
Local aArea   := GetArea()
Local aNwInfo := ParamIxb[1]                                                  
Local nX

For nX := 1 to Len(aNwInfo)
   aNwInfo[nX] := WinGetVal(nX, aNwInfo[nX])                           
Next

RestArea(aArea)
Return(aNwInfo)